#include <iostream>
#include <cmath>
#include <cstdlib>
#include "linked_list.h"

int main()
{
	auto* list = new linkedList;
	auto* Arr = new linkedList;
	auto* ptr = new Node;
	for (int i = 0; i < 6; i++)
		list->insertByTail(i + 1);
    //list->Display();
} 
